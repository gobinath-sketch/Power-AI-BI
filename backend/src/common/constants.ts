/** Max rows pulled from Power BI per report (query-level cap). */
export const MAX_ROWS_LIMIT = 8000;

/** AI insight generation timeout (ms). */
export const AI_TIMEOUT_MS = 3000;

/** Max wait when dataset refresh is in progress (ms). */
export const REFRESH_WAIT_MS = 20000;

/** Chart: max categories before collapsing to "Other". */
export const CHART_TOP_N = 12;

/** Time series downsample target points. */
export const TIMESERIES_MAX_POINTS = 200;
